<h1>Listing position_thresholds</h1>

<table>
  <tr>
  </tr>

<% @position_thresholds.each do |position_threshold| %>
  <tr>
    <td><%= link_to 'Show', position_threshold %></td>
    <td><%= link_to 'Edit', edit_position_threshold_path(position_threshold) %></td>
    <td><%= link_to 'Destroy', position_threshold, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New position_threshold', new_position_threshold_path %>